import { Dialog, DialogTitle, DialogContent, Divider, Box, Button, Typography, Chip, Stack } from '@mui/material';
import { NpcDialogProps } from "@/interfaces/npc.interface";

export default function NpcDetailsDialog({ open, onClose, npc }: NpcDialogProps) {
    return(
        <p>Content</p>
    )
}