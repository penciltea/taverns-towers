// For use with both settlements and sites that are created in "wilderness" (detached from settlements)

export interface EnvironmentInterface {
  climate: string;
  terrain: string[];
  tags: string[];
}