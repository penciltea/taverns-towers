import { MenuRuleFn } from "../common/menu.rules";


// Export the array of rules for tavern menu filtering
export const tavernMenuRules: MenuRuleFn[] = [
  //filterByClimate,
  //filterByTerrain,
  // Add more tavern-specific rules here
];